# 15_minio_spark_iceberg_polaris-postgres_kafka-connect

This step corresponds to the Docker Compose setup for **minio + spark + iceberg + (polaris + postgres) + kafka-connect**.

## Location

The actual Docker Compose files are located [here](../docker/15_minio_spark_iceberg_polaris-postgres_kafka-connect/docker-compose.yaml).
