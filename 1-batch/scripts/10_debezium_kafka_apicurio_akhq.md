# 10_debezium_kafka_apicurio_akhq

This step corresponds to the Docker Compose setup for **Debezium + Kafka + Apicurio + AKHQ**.

## Location

The actual Docker Compose files are located [here](../docker/10_debezium_kafka_apicurio_akhq/docker-compose.yaml).

## Usage
```pwsh
docker compose up -d
```
